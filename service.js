import Browser from './browser';
import models from './models/index';

const { UserModel } = models;

console.log(UserModel);
// UserModel.findAll().then(res => console.log(res));

export default {
	// startGetToken: async () => {
	// 	const inforAccounts = [];

	// 	inforAccounts.
	// 	await Browser.getMultiToken(inforAccounts);

	// }
}